#!/usr/bin/env ruby

require 'optparse'
require 'slack_log_viewer'

options = { log_dir: '.' }

parser = OptionParser.new
parser.on('-d VALUE') { |dir| options[:log_dir] = dir }
parser.parse!

SlackLogViewer.set :log_dir, options[:log_dir]
SlackLogViewer.run!
